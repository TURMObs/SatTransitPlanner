# --- Search engine (python -m sattransit) -------------------------------------
skyfield>=1.49
numpy>=1.24

# --- Viewer (python -m sattransit.gui) ----------------------------------------
# Only needed for the GUI; the search runs without it.
PyQt6>=6.6

# Nothing else is needed. The macOS window frame is matched to the theme through
# the Objective-C runtime directly, so PyObjC is not required (it is used if it
# happens to be installed).
